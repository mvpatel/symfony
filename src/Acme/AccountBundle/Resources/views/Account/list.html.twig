<div class="fos_user_group_list">
    <ul>
    {% for user in users %}
        <li><a href="{{ path('updateuser', {'id': user.getId()} ) }}">{{ user.getEmail() }}</a></li>
    {% endfor %}
    </ul>
</div>
