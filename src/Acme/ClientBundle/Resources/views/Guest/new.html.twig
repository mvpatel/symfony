{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Guest creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('guest') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
